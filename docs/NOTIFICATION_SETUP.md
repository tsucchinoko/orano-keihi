# 通知機能設定ガイド

## 概要

このドキュメントでは、CI/CDパイプラインの通知機能の設定方法について説明します。通知機能により、リリース完了やビルド失敗などの重要なイベントを関係者に自動的に通知できます。

## 通知の種類

### 1. GitHub内通知

#### 自動作成されるIssue
- **リリース完了通知**: 成功したリリースの詳細情報
- **ビルド失敗通知**: MacOSまたはWindowsビルドの失敗詳細
- **部分的ビルド失敗通知**: 一部プラットフォームでの失敗
- **ワークフロー統合報告**: 全体的な実行結果サマリー

#### 通知内容
- ビルド統計情報
- 成果物の詳細（ファイル名、サイズ）
- 関係者への自動メンション
- 次のステップのチェックリスト
- 関連リンク（リリースページ、ワークフロー実行ログ）

### 2. 外部通知システム（オプション）

以下の外部システムへの通知が可能です：
- Slack
- Discord
- Microsoft Teams

## 外部通知の設定

### Slack通知の設定

1. **Slack Webhookの作成**
   ```bash
   # Slackワークスペースで Incoming Webhooks アプリを追加
   # Webhook URLを取得
   ```

2. **GitHub Secretsの設定**
   ```bash
   # リポジトリの Settings > Secrets and variables > Actions で設定
   SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
   ```

3. **通知内容**
   - リリース完了時の詳細情報
   - ビルド統計とファイル情報
   - リリースページへの直接リンク
   - インタラクティブなボタン

### Discord通知の設定

1. **Discord Webhookの作成**
   ```bash
   # Discordサーバーの設定 > 連携サービス > ウェブフック で作成
   # Webhook URLを取得
   ```

2. **GitHub Secretsの設定**
   ```bash
   DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR/WEBHOOK/URL
   ```

3. **通知内容**
   - カラー付きEmbed形式
   - リリース情報の構造化表示
   - タイムスタンプ付き
   - 関係者情報

### Microsoft Teams通知の設定

1. **Teams Webhookの作成**
   ```bash
   # Teamsチャンネルで コネクタ > Incoming Webhook を追加
   # Webhook URLを取得
   ```

2. **GitHub Secretsの設定**
   ```bash
   TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/YOUR/WEBHOOK/URL
   ```

3. **通知内容**
   - MessageCard形式
   - アクションボタン付き
   - 構造化された情報表示

## 通知のカスタマイズ

### 関係者の自動検出

システムは以下の方法で関係者を自動検出します：

1. **リリース作成者**: `context.actor`
2. **プッシュ実行者**: `context.payload.pusher.name`
3. **コミット作成者**: `context.payload.head_commit.author.username`

### 通知タイミング

- **リリース完了時**: すべてのビルドが成功し、リリースが作成された時
- **ビルド失敗時**: MacOSまたはWindowsビルドが失敗した時
- **部分的失敗時**: 一部のプラットフォームでビルドが失敗した時
- **ワークフロー完了時**: 全体的なワークフロー実行が完了した時

## トラブルシューティング

### 通知が送信されない場合

1. **GitHub Secretsの確認**
   ```bash
   # Settings > Secrets and variables > Actions で以下を確認
   - SLACK_WEBHOOK_URL
   - DISCORD_WEBHOOK_URL  
   - TEAMS_WEBHOOK_URL
   ```

2. **Webhook URLの有効性確認**
   ```bash
   # 各サービスでWebhookが有効かテスト送信で確認
   ```

3. **権限の確認**
   ```bash
   # GitHub Actionsに必要な権限が付与されているか確認
   # Settings > Actions > General > Workflow permissions
   ```

### 通知内容の調整

通知内容をカスタマイズしたい場合は、`.github/workflows/release.yml`の以下のセクションを編集してください：

- `sendAdditionalNotifications()` 関数
- `sendWorkflowSummaryNotifications()` 関数

### ログの確認

通知の送信状況は、GitHub Actionsのログで確認できます：

```bash
# ワークフロー実行ログで以下のセクションを確認
📢 追加通知の送信結果:
   Slack通知: ✅ 送信成功
   Discord通知: ✅ 送信成功
```

## セキュリティ考慮事項

1. **Webhook URLの管理**
   - Webhook URLは機密情報として扱う
   - GitHub Secretsを使用して安全に管理
   - 定期的にWebhookを更新

2. **通知内容の制限**
   - 機密情報を通知に含めない
   - 必要最小限の情報のみ送信

3. **アクセス制御**
   - 通知を受信するチャンネル/ユーザーを適切に制限
   - 不要になったWebhookは無効化

## 参考リンク

- [GitHub Actions Secrets](https://docs.github.com/en/actions/security-guides/encrypted-secrets)
- [Slack Incoming Webhooks](https://api.slack.com/messaging/webhooks)
- [Discord Webhooks](https://discord.com/developers/docs/resources/webhook)
- [Microsoft Teams Webhooks](https://docs.microsoft.com/en-us/microsoftteams/platform/webhooks-and-connectors/how-to/add-incoming-webhook)