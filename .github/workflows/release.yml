name: MacOS Release Build

# mainブランチプッシュ時にトリガー
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# 環境変数の設定
env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full
  RUST_LOG: debug

jobs:
  build-macos:
    # MacOSランナー環境の設定
    runs-on: macos-latest
    
    steps:
    - name: リポジトリのチェックアウト
      uses: actions/checkout@v4
      
    - name: ビルド情報の出力
      run: |
        echo "ビルド開始: $(date)"
        echo "コミットハッシュ: ${{ github.sha }}"
        echo "ブランチ: ${{ github.ref_name }}"
        
    # Node.js/pnpm環境セットアップ
    - name: Node.jsのセットアップ
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: pnpmのセットアップ
      uses: pnpm/action-setup@v4
      with:
        version: 8
        
    - name: pnpmキャッシュの設定
      uses: actions/cache@v4
      with:
        path: |
          ~/.pnpm-store
          node_modules
        key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
        restore-keys: |
          ${{ runner.os }}-pnpm-
          
    - name: フロントエンド依存関係のインストール
      run: |
        echo "pnpmの依存関係を確認中..."
        pnpm --version
        echo "利用可能なスクリプトを確認..."
        pnpm run --help || echo "スクリプトヘルプの表示に失敗"
        
        echo "pnpmの依存関係をインストール中..."
        # pnpmの依存関係をインストール
        pnpm install --frozen-lockfile || echo "依存関係のインストールに失敗"
        
    - name: ビルドヘルパースクリプトの実行権限設定
      run: chmod +x .github/scripts/build-helper.sh
      
    - name: フロントエンドビルドの実行
      id: frontend-build
      run: |
        # ヘルパースクリプトを使用してフロントエンドビルドを実行
        source .github/scripts/build-helper.sh
        build_frontend
      continue-on-error: false
      
    - name: フロントエンドビルド失敗時の詳細ログ
      if: failure() && steps.frontend-build.outcome == 'failure'
      run: |
        echo "=== フロントエンドビルド失敗の詳細 ==="
        echo "ビルドログ:"
        cat frontend-build.log || echo "ビルドログファイルが見つかりません"
        echo "=== package.jsonの確認 ==="
        cat package.json || echo "package.jsonが見つかりません"
        echo "=== pnpmの設定確認 ==="
        cat pnpm-lock.yaml | head -20 || echo "pnpm-lock.yamlが見つかりません"
        echo "=== ファイル構造確認 ==="
        find . -name "*.svelte" -o -name "*.ts" -o -name "*.js" | head -20
        
    # Rust環境セットアップ
    - name: Rustツールチェーンのセットアップ
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        
    - name: Tauri CLIのインストール
      uses: nick-fields/retry@v3
      with:
        timeout_minutes: 10
        max_attempts: 3
        retry_on: error
        command: |
          cargo install tauri-cli --version "^2.0" --locked
        
    - name: Cargoキャッシュの設定
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          src-tauri/target/
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
          
    - name: Tauriビルド環境の準備
      id: tauri-setup
      run: |
        echo "Rustバージョン確認..."
        rustc --version
        cargo --version
        echo "Tauriビルド環境を準備中..."
        # Tauriの依存関係を事前にチェック
        cd src-tauri
        echo "Cargo.tomlの確認..."
        cat Cargo.toml
        echo "依存関係のチェック..."
        cargo check 2>&1 | tee ../tauri-setup.log
        echo "Tauriビルド環境の準備が完了しました"
      continue-on-error: false
      
    - name: Tauriセットアップ失敗時の詳細ログ
      if: failure() && steps.tauri-setup.outcome == 'failure'
      run: |
        echo "=== Tauriセットアップ失敗の詳細 ==="
        echo "セットアップログ:"
        cat tauri-setup.log || echo "セットアップログファイルが見つかりません"
        echo "=== Cargo.lockの確認 ==="
        cat src-tauri/Cargo.lock | head -50 || echo "Cargo.lockが見つかりません"
        echo "=== tauri.conf.jsonの確認 ==="
        cat src-tauri/tauri.conf.json || echo "tauri.conf.jsonが見つかりません"
        
    # macOS署名設定
    - name: macOS署名環境の設定
      id: signing-setup
      run: |
        echo "macOS署名環境を設定中..."
        
        # 開発用署名の設定
        # 注意: プロダクション環境では適切な証明書を使用してください
        echo "開発用署名設定を適用中..."
        
        # Keychainの設定（開発用）
        if [ -n "${{ secrets.MACOS_CERTIFICATE }}" ] && [ -n "${{ secrets.MACOS_CERTIFICATE_PWD }}" ]; then
          echo "証明書が設定されています。署名を有効化します。"
          
          # 証明書をKeychainにインポート
          echo "${{ secrets.MACOS_CERTIFICATE }}" | base64 --decode > certificate.p12
          
          # 一時的なKeychainを作成
          security create-keychain -p "${{ secrets.KEYCHAIN_PASSWORD || 'temp-password' }}" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "${{ secrets.KEYCHAIN_PASSWORD || 'temp-password' }}" build.keychain
          
          # 証明書をインポート
          security import certificate.p12 -k build.keychain -P "${{ secrets.MACOS_CERTIFICATE_PWD }}" -T /usr/bin/codesign
          
          # 証明書の信頼設定
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "${{ secrets.KEYCHAIN_PASSWORD || 'temp-password' }}" build.keychain
          
          echo "signing_enabled=true" >> $GITHUB_OUTPUT
          echo "署名が有効化されました"
          
          # 利用可能な署名IDを確認
          echo "利用可能な署名ID:"
          security find-identity -v -p codesigning build.keychain || echo "署名IDが見つかりません"
          
        else
          echo "証明書が設定されていません。開発用署名を無効化します。"
          echo "signing_enabled=false" >> $GITHUB_OUTPUT
          
          # 署名を無効化するためにTauri設定を一時的に変更
          cd src-tauri
          cp tauri.conf.json tauri.conf.json.backup
          
          # 署名設定を無効化
          cat tauri.conf.json | jq '.bundle.macOS.signingIdentity = null' > tauri.conf.json.tmp
          mv tauri.conf.json.tmp tauri.conf.json
          
          echo "開発用ビルドとして署名なしで実行します"
        fi
        
        echo "署名環境設定完了"
      continue-on-error: false
      
    - name: 署名設定失敗時の詳細ログ
      if: failure() && steps.signing-setup.outcome == 'failure'
      run: |
        echo "=== 署名設定失敗の詳細 ==="
        echo "Keychain情報:"
        security list-keychains || echo "Keychain一覧取得失敗"
        echo "証明書情報:"
        security find-identity -v || echo "証明書一覧取得失敗"
        echo "Tauri設定:"
        cat src-tauri/tauri.conf.json | grep -A 10 -B 5 "signingIdentity" || echo "署名設定が見つかりません"
        
    # Tauriアプリケーションビルド
    - name: Tauriアプリケーションのビルド
      id: tauri-build
      run: |
        # ヘルパースクリプトを使用してTauriビルドを実行
        source .github/scripts/build-helper.sh
        build_tauri
      continue-on-error: false
      
    - name: Tauriビルド失敗時の詳細ログ
      if: failure() && steps.tauri-build.outcome == 'failure'
      run: |
        echo "=== Tauriビルド失敗の詳細 ==="
        echo "ビルドログ:"
        cat tauri-build.log || echo "ビルドログファイルが見つかりません"
        echo "=== コンパイルエラーの詳細 ==="
        find src-tauri/target -name "*.log" -type f -exec echo "=== {} ===" \; -exec cat {} \; || echo "追加ログファイルが見つかりません"
        echo "=== 環境変数の確認 ==="
        env | grep -E "(RUST|CARGO|TAURI)" || echo "関連環境変数が見つかりません"
        
    - name: ビルド成果物の確認
      id: artifact-check
      run: |
        # ヘルパースクリプトを使用して成果物を確認
        source .github/scripts/build-helper.sh
        if verify_artifacts; then
          echo "artifact_exists=true" >> $GITHUB_OUTPUT
        else
          echo "artifact_exists=false" >> $GITHUB_OUTPUT
          exit 1
        fi
      continue-on-error: false
      
    # 署名検証
    - name: 署名済み実行可能ファイルの検証
      if: steps.artifact-check.outputs.artifact_exists == 'true'
      id: signature-verification
      run: |
        echo "署名検証を開始します..."
        
        # dmgファイルのパスを取得
        DMG_PATH=$(find src-tauri/target/release/bundle/dmg -name "*.dmg" -type f | head -1)
        
        if [ -z "$DMG_PATH" ]; then
          echo "::error::dmgファイルが見つかりません"
          exit 1
        fi
        
        echo "検証対象dmgファイル: $DMG_PATH"
        
        # dmgファイルの署名状況を確認
        echo "=== dmgファイルの署名確認 ==="
        if codesign -dv "$DMG_PATH" 2>&1; then
          echo "dmgファイルに署名が検出されました"
          echo "dmg_signed=true" >> $GITHUB_OUTPUT
          
          # 署名の詳細情報を取得
          echo "署名詳細情報:"
          codesign -dvv "$DMG_PATH" 2>&1 || echo "署名詳細取得失敗"
          
          # 署名の検証
          echo "署名検証実行中..."
          if codesign --verify --deep --strict "$DMG_PATH" 2>&1; then
            echo "✅ dmgファイルの署名検証に成功しました"
            echo "dmg_verification=success" >> $GITHUB_OUTPUT
          else
            echo "⚠️ dmgファイルの署名検証に失敗しました"
            echo "dmg_verification=failed" >> $GITHUB_OUTPUT
          fi
          
        else
          echo "dmgファイルに署名が見つかりません（開発用ビルド）"
          echo "dmg_signed=false" >> $GITHUB_OUTPUT
          echo "dmg_verification=unsigned" >> $GITHUB_OUTPUT
        fi
        
        # dmgファイルをマウントしてアプリケーションの署名を確認
        echo ""
        echo "=== アプリケーション内部の署名確認 ==="
        
        # 一時的なマウントポイントを作成
        MOUNT_POINT="/tmp/dmg_mount_$$"
        mkdir -p "$MOUNT_POINT"
        
        # dmgファイルをマウント
        if hdiutil attach "$DMG_PATH" -mountpoint "$MOUNT_POINT" -nobrowse -quiet; then
          echo "dmgファイルをマウントしました: $MOUNT_POINT"
          
          # アプリケーションファイルを探す
          APP_PATH=$(find "$MOUNT_POINT" -name "*.app" -type d | head -1)
          
          if [ -n "$APP_PATH" ]; then
            echo "アプリケーション発見: $APP_PATH"
            
            # アプリケーションの署名確認
            if codesign -dv "$APP_PATH" 2>&1; then
              echo "アプリケーションに署名が検出されました"
              echo "app_signed=true" >> $GITHUB_OUTPUT
              
              # アプリケーション署名の詳細情報
              echo "アプリケーション署名詳細:"
              codesign -dvv "$APP_PATH" 2>&1 || echo "アプリケーション署名詳細取得失敗"
              
              # アプリケーション署名の検証
              echo "アプリケーション署名検証実行中..."
              if codesign --verify --deep --strict "$APP_PATH" 2>&1; then
                echo "✅ アプリケーションの署名検証に成功しました"
                echo "app_verification=success" >> $GITHUB_OUTPUT
              else
                echo "⚠️ アプリケーションの署名検証に失敗しました"
                echo "app_verification=failed" >> $GITHUB_OUTPUT
              fi
              
              # Entitlementsの確認
              echo "Entitlements確認中..."
              codesign -d --entitlements :- "$APP_PATH" 2>/dev/null || echo "Entitlements情報なし"
              
            else
              echo "アプリケーションに署名が見つかりません（開発用ビルド）"
              echo "app_signed=false" >> $GITHUB_OUTPUT
              echo "app_verification=unsigned" >> $GITHUB_OUTPUT
            fi
            
            # アプリケーションの実行可能性を確認
            echo ""
            echo "=== 実行可能性の確認 ==="
            
            # 実行可能ファイルの権限確認
            EXECUTABLE_PATH="$APP_PATH/Contents/MacOS"
            if [ -d "$EXECUTABLE_PATH" ]; then
              echo "実行可能ファイルディレクトリ: $EXECUTABLE_PATH"
              ls -la "$EXECUTABLE_PATH"
              
              # 実行可能ファイルを探す
              MAIN_EXECUTABLE=$(find "$EXECUTABLE_PATH" -type f -perm +111 | head -1)
              if [ -n "$MAIN_EXECUTABLE" ]; then
                echo "メイン実行可能ファイル: $MAIN_EXECUTABLE"
                
                # ファイル情報を確認
                file "$MAIN_EXECUTABLE" || echo "ファイル情報取得失敗"
                
                # 依存関係を確認
                echo "依存関係確認:"
                otool -L "$MAIN_EXECUTABLE" 2>/dev/null | head -10 || echo "依存関係確認失敗"
                
                echo "executable_found=true" >> $GITHUB_OUTPUT
              else
                echo "::warning::実行可能ファイルが見つかりません"
                echo "executable_found=false" >> $GITHUB_OUTPUT
              fi
            else
              echo "::warning::実行可能ファイルディレクトリが見つかりません"
              echo "executable_found=false" >> $GITHUB_OUTPUT
            fi
            
          else
            echo "::error::dmg内にアプリケーションが見つかりません"
            echo "app_signed=false" >> $GITHUB_OUTPUT
            echo "app_verification=not_found" >> $GITHUB_OUTPUT
            echo "executable_found=false" >> $GITHUB_OUTPUT
          fi
          
          # dmgファイルをアンマウント
          hdiutil detach "$MOUNT_POINT" -quiet || echo "アンマウント失敗"
          
        else
          echo "::error::dmgファイルのマウントに失敗しました"
          echo "app_signed=false" >> $GITHUB_OUTPUT
          echo "app_verification=mount_failed" >> $GITHUB_OUTPUT
          echo "executable_found=false" >> $GITHUB_OUTPUT
        fi
        
        # 一時ディレクトリを削除
        rm -rf "$MOUNT_POINT" 2>/dev/null || echo "一時ディレクトリ削除失敗"
        
        # 署名設定の復元（署名が無効化されていた場合）
        if [ "${{ steps.signing-setup.outputs.signing_enabled }}" = "false" ]; then
          echo "署名設定を復元中..."
          cd src-tauri
          if [ -f tauri.conf.json.backup ]; then
            mv tauri.conf.json.backup tauri.conf.json
            echo "Tauri設定を復元しました"
          fi
        fi
        
        # 一時的なKeychainをクリーンアップ
        if [ "${{ steps.signing-setup.outputs.signing_enabled }}" = "true" ]; then
          echo "Keychainクリーンアップ中..."
          security delete-keychain build.keychain 2>/dev/null || echo "Keychainクリーンアップ完了"
          rm -f certificate.p12 2>/dev/null || echo "証明書ファイルクリーンアップ完了"
        fi
        
        echo "署名検証完了"
      continue-on-error: false
      
    - name: 署名検証失敗時の詳細ログ
      if: failure() && steps.signature-verification.outcome == 'failure'
      run: |
        echo "=== 署名検証失敗の詳細 ==="
        echo "署名設定状況:"
        echo "  - 署名有効: ${{ steps.signing-setup.outputs.signing_enabled }}"
        echo "  - dmg署名: ${{ steps.signature-verification.outputs.dmg_signed }}"
        echo "  - dmg検証: ${{ steps.signature-verification.outputs.dmg_verification }}"
        echo "  - アプリ署名: ${{ steps.signature-verification.outputs.app_signed }}"
        echo "  - アプリ検証: ${{ steps.signature-verification.outputs.app_verification }}"
        echo "  - 実行可能ファイル: ${{ steps.signature-verification.outputs.executable_found }}"
        
        echo "システム署名環境:"
        security list-keychains || echo "Keychain一覧取得失敗"
        security find-identity -v || echo "署名ID一覧取得失敗"
        
        echo "Tauri設定:"
        cat src-tauri/tauri.conf.json | grep -A 15 -B 5 "macOS" || echo "macOS設定が見つかりません"
        
    # 署名検証結果の報告
    - name: 署名検証結果の報告
      if: steps.artifact-check.outputs.artifact_exists == 'true'
      run: |
        echo "=== 署名検証結果レポート ==="
        echo "実行日時: $(date -Iseconds)"
        echo ""
        
        # 署名設定状況
        if [ "${{ steps.signing-setup.outputs.signing_enabled }}" = "true" ]; then
          echo "✅ 署名設定: 有効（証明書使用）"
        else
          echo "ℹ️ 署名設定: 無効（開発用ビルド）"
        fi
        
        # dmgファイル署名状況
        case "${{ steps.signature-verification.outputs.dmg_verification }}" in
          "success")
            echo "✅ dmgファイル署名: 検証成功"
            ;;
          "failed")
            echo "❌ dmgファイル署名: 検証失敗"
            ;;
          "unsigned")
            echo "ℹ️ dmgファイル署名: 署名なし（開発用）"
            ;;
          *)
            echo "⚠️ dmgファイル署名: 不明な状態"
            ;;
        esac
        
        # アプリケーション署名状況
        case "${{ steps.signature-verification.outputs.app_verification }}" in
          "success")
            echo "✅ アプリケーション署名: 検証成功"
            ;;
          "failed")
            echo "❌ アプリケーション署名: 検証失敗"
            ;;
          "unsigned")
            echo "ℹ️ アプリケーション署名: 署名なし（開発用）"
            ;;
          "not_found")
            echo "❌ アプリケーション署名: アプリケーション未発見"
            ;;
          "mount_failed")
            echo "❌ アプリケーション署名: dmgマウント失敗"
            ;;
          *)
            echo "⚠️ アプリケーション署名: 不明な状態"
            ;;
        esac
        
        # 実行可能ファイル状況
        if [ "${{ steps.signature-verification.outputs.executable_found }}" = "true" ]; then
          echo "✅ 実行可能ファイル: 発見・確認済み"
        else
          echo "❌ 実行可能ファイル: 未発見または問題あり"
        fi
        
        echo ""
        echo "=== 要件2.2の充足確認 ==="
        echo "要件: エンドユーザーがdmgファイルをダウンロードする THEN システムは署名済みの実行可能なアプリケーションを提供する"
        echo ""
        
        # 要件充足の判定
        if [ "${{ steps.signature-verification.outputs.executable_found }}" = "true" ]; then
          if [ "${{ steps.signature-verification.outputs.app_verification }}" = "success" ]; then
            echo "✅ 要件2.2は完全に満たされています（署名済み実行可能ファイル）"
          elif [ "${{ steps.signature-verification.outputs.app_verification }}" = "unsigned" ]; then
            echo "⚠️ 要件2.2は部分的に満たされています（実行可能だが署名なし - 開発用）"
            echo "   プロダクション環境では適切な証明書を設定してください"
          else
            echo "❌ 要件2.2は満たされていません（署名検証失敗）"
          fi
        else
          echo "❌ 要件2.2は満たされていません（実行可能ファイル未発見）"
        fi
        
        echo ""
        echo "=== 署名設定の推奨事項 ==="
        if [ "${{ steps.signing-setup.outputs.signing_enabled }}" = "false" ]; then
          echo "プロダクション環境での署名を有効化するには、以下の秘密を設定してください:"
          echo "  - MACOS_CERTIFICATE: Apple Developer証明書（Base64エンコード済み）"
          echo "  - MACOS_CERTIFICATE_PWD: 証明書のパスワード"
          echo "  - KEYCHAIN_PASSWORD: 一時Keychainのパスワード（オプション）"
          echo ""
          echo "証明書の取得方法:"
          echo "  1. Apple Developer Programに登録"
          echo "  2. 'Developer ID Application'証明書を作成"
          echo "  3. Keychain Accessから.p12形式でエクスポート"
          echo "  4. Base64エンコードしてGitHub Secretsに保存"
        else
          echo "署名設定は正常に構成されています"
        fi
        
        echo ""
        echo "=== レポート終了 ==="
        
    # dmgファイルの成果物アップロード
    - name: dmgファイルのアップロード
      if: steps.artifact-check.outputs.artifact_exists == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: macos-dmg-${{ github.sha }}
        path: src-tauri/target/release/bundle/dmg/*.dmg
        if-no-files-found: error
        retention-days: 30
        
    - name: 成果物アップロード失敗時の詳細ログ
      if: failure() && steps.artifact-check.outputs.artifact_exists == 'false'
      run: |
        echo "=== 成果物生成失敗の詳細 ==="
        echo "バンドルプロセスの確認..."
        find src-tauri/target/release -type f -name "*" | grep -E "\.(app|dmg|pkg)$" || echo "MacOS用成果物が見つかりません"
        echo "=== ビルド設定の確認 ==="
        cat src-tauri/tauri.conf.json | grep -A 20 -B 5 "bundle" || echo "バンドル設定が見つかりません"
        
    # ログファイルの収集とアップロード
    - name: ビルドログの収集
      if: always()  # 成功・失敗に関わらず実行
      run: |
        echo "ビルドログを収集中..."
        mkdir -p build-logs
        
        # 各ステップのログファイルを収集
        cp frontend-build.log build-logs/ 2>/dev/null || echo "フロントエンドビルドログなし"
        cp tauri-setup.log build-logs/ 2>/dev/null || echo "Tauriセットアップログなし"
        cp tauri-build.log build-logs/ 2>/dev/null || echo "Tauriビルドログなし"
        
        # リリースノートを収集（成功時のみ）
        cp release-notes.md build-logs/ 2>/dev/null || echo "リリースノートなし"
        
        # システム情報を記録
        echo "=== システム情報 ===" > build-logs/system-info.log
        echo "日時: $(date)" >> build-logs/system-info.log
        echo "OS: $(uname -a)" >> build-logs/system-info.log
        echo "Rust: $(rustc --version)" >> build-logs/system-info.log
        echo "Cargo: $(cargo --version)" >> build-logs/system-info.log
        echo "pnpm: $(pnpm --version)" >> build-logs/system-info.log
        echo "Node.js: $(node --version)" >> build-logs/system-info.log
        echo "ディスク使用量:" >> build-logs/system-info.log
        df -h >> build-logs/system-info.log
        echo "メモリ使用量:" >> build-logs/system-info.log
        vm_stat >> build-logs/system-info.log 2>/dev/null || echo "メモリ情報取得失敗" >> build-logs/system-info.log
        echo "pnpm設定:" >> build-logs/system-info.log
        cat .pnpmrc >> build-logs/system-info.log 2>/dev/null || echo ".pnpmrcが見つかりません" >> build-logs/system-info.log
        
        # Cargoの詳細ログを収集
        find src-tauri/target -name "*.log" -type f -exec cp {} build-logs/ \; 2>/dev/null || echo "追加Cargoログなし"
        
        # pnpm設定情報を記録
        echo "=== pnpm設定情報 ===" >> build-logs/system-info.log
        pnpm config list >> build-logs/system-info.log 2>/dev/null || echo "pnpm設定取得失敗" >> build-logs/system-info.log
        
        # リリース情報を記録（mainブランチの場合）
        if [ "${{ github.ref_name }}" = "main" ] && [ "${{ github.event_name }}" = "push" ]; then
          echo "=== リリース情報 ===" > build-logs/release-info.log
          echo "リリースタグ: ${{ steps.version.outputs.release_tag || 'N/A' }}" >> build-logs/release-info.log
          echo "リリース名: ${{ steps.version.outputs.release_name || 'N/A' }}" >> build-logs/release-info.log
          echo "dmgファイル: ${{ steps.dmg-path.outputs.dmg_name || 'N/A' }}" >> build-logs/release-info.log
          echo "ファイルサイズ: ${{ steps.dmg-path.outputs.dmg_size || 'N/A' }}" >> build-logs/release-info.log
          echo "リリース作成: ${{ steps.check-release.outputs.release_exists == 'false' && '成功' || '既存リリース存在' }}" >> build-logs/release-info.log
        fi
        
        echo "ログ収集完了"
        ls -la build-logs/
        
    - name: ビルドログのアップロード
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-${{ github.sha }}
        path: build-logs/
        retention-days: 7
        
    - name: ビルド失敗時の通知
      if: failure()
      run: |
        echo "::error title=ビルド失敗::MacOSビルドプロセスが失敗しました"
        echo "::error::詳細なログはbuild-logs-${{ github.sha }}成果物を確認してください"
        echo "::error::失敗したコミット: ${{ github.sha }}"
        echo "::error::失敗したブランチ: ${{ github.ref_name }}"
        
        # GitHub Issues APIを使用した通知（オプション）
        if [ "${{ github.event_name }}" = "push" ] && [ "${{ github.ref_name }}" = "main" ]; then
          echo "mainブランチでのビルド失敗を検出しました"
          echo "::warning::mainブランチでビルドが失敗しました。至急確認が必要です。"
        fi
        
    # GitHubリリース自動化
    - name: バージョン情報の取得
      if: success() && github.ref_name == 'main' && github.event_name == 'push'
      id: version
      run: |
        # package.jsonからバージョンを取得
        PACKAGE_VERSION=$(cat package.json | grep '"version"' | head -1 | awk -F: '{ print $2 }' | sed 's/[",]//g' | tr -d ' ')
        echo "package_version=$PACKAGE_VERSION" >> $GITHUB_OUTPUT
        
        # Tauri設定からバージョンを取得
        TAURI_VERSION=$(cat src-tauri/tauri.conf.json | grep '"version"' | head -1 | awk -F: '{ print $2 }' | sed 's/[",]//g' | tr -d ' ')
        echo "tauri_version=$TAURI_VERSION" >> $GITHUB_OUTPUT
        
        # リリースタグを生成（v接頭辞付き）
        RELEASE_TAG="v$PACKAGE_VERSION"
        echo "release_tag=$RELEASE_TAG" >> $GITHUB_OUTPUT
        
        # リリース名を生成
        RELEASE_NAME="オラの経費だゾ $RELEASE_TAG"
        echo "release_name=$RELEASE_NAME" >> $GITHUB_OUTPUT
        
        echo "バージョン情報:"
        echo "Package Version: $PACKAGE_VERSION"
        echo "Tauri Version: $TAURI_VERSION"
        echo "Release Tag: $RELEASE_TAG"
        echo "Release Name: $RELEASE_NAME"
        
    - name: dmgファイルパスの取得
      if: success() && github.ref_name == 'main' && github.event_name == 'push'
      id: dmg-path
      run: |
        # dmgファイルのパスを取得
        DMG_PATH=$(find src-tauri/target/release/bundle/dmg -name "*.dmg" -type f | head -1)
        if [ -z "$DMG_PATH" ]; then
          echo "::error::dmgファイルが見つかりません"
          exit 1
        fi
        
        DMG_NAME=$(basename "$DMG_PATH")
        echo "dmg_path=$DMG_PATH" >> $GITHUB_OUTPUT
        echo "dmg_name=$DMG_NAME" >> $GITHUB_OUTPUT
        
        # ファイルサイズを取得
        DMG_SIZE=$(ls -lh "$DMG_PATH" | awk '{print $5}')
        echo "dmg_size=$DMG_SIZE" >> $GITHUB_OUTPUT
        
        echo "dmgファイル情報:"
        echo "Path: $DMG_PATH"
        echo "Name: $DMG_NAME"
        echo "Size: $DMG_SIZE"
        
    - name: 既存リリースの確認
      if: success() && github.ref_name == 'main' && github.event_name == 'push'
      id: check-release
      run: |
        # 既存のリリースタグが存在するかチェック
        RELEASE_TAG="${{ steps.version.outputs.release_tag }}"
        
        # GitHub APIを使用してリリースの存在を確認
        RELEASE_EXISTS=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          "https://api.github.com/repos/${{ github.repository }}/releases/tags/$RELEASE_TAG" \
          | jq -r '.tag_name // empty')
        
        if [ -n "$RELEASE_EXISTS" ]; then
          echo "release_exists=true" >> $GITHUB_OUTPUT
          echo "既存のリリース $RELEASE_TAG が見つかりました"
        else
          echo "release_exists=false" >> $GITHUB_OUTPUT
          echo "新しいリリース $RELEASE_TAG を作成します"
        fi
        
    - name: リリースノートの自動生成
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      id: generate-release-notes
      run: |
        # 前回のリリースタグを取得
        PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
        
        # リリースノートのベース部分を作成
        RELEASE_NOTES="## 🎉 新しいリリース: ${{ steps.version.outputs.release_tag }}

        ### 📦 ダウンロード
        - **MacOS**: ${{ steps.dmg-path.outputs.dmg_name }} (${{ steps.dmg-path.outputs.dmg_size }})

        ### 📋 ビルド情報
        - **コミット**: ${{ github.sha }}
        - **ビルド日時**: $(date -Iseconds)
        - **ビルド者**: ${{ github.actor }}"
        
        # コミットメッセージから変更履歴を生成
        if [ -n "$PREVIOUS_TAG" ]; then
          echo "前回のリリース: $PREVIOUS_TAG"
          
          # 前回のリリースから今回までのコミットを取得
          COMMITS=$(git log --pretty=format:"- %s (%an)" ${PREVIOUS_TAG}..HEAD --no-merges)
          
          if [ -n "$COMMITS" ]; then
            RELEASE_NOTES="$RELEASE_NOTES

        ### 📝 変更履歴
        $COMMITS"
          fi
          
          # 機能追加、バグ修正、その他に分類
          FEATURES=$(git log --pretty=format:"- %s" ${PREVIOUS_TAG}..HEAD --no-merges --grep="feat\|feature\|add\|新機能\|追加" --grep="^feat" --grep="^add")
          FIXES=$(git log --pretty=format:"- %s" ${PREVIOUS_TAG}..HEAD --no-merges --grep="fix\|bug\|修正\|バグ" --grep="^fix")
          OTHERS=$(git log --pretty=format:"- %s" ${PREVIOUS_TAG}..HEAD --no-merges --invert-grep --grep="feat\|feature\|add\|新機能\|追加\|fix\|bug\|修正\|バグ")
          
          if [ -n "$FEATURES" ]; then
            RELEASE_NOTES="$RELEASE_NOTES

        ### ✨ 新機能・改善
        $FEATURES"
          fi
          
          if [ -n "$FIXES" ]; then
            RELEASE_NOTES="$RELEASE_NOTES

        ### 🐛 バグ修正
        $FIXES"
          fi
          
          if [ -n "$OTHERS" ]; then
            RELEASE_NOTES="$RELEASE_NOTES

        ### 🔧 その他の変更
        $OTHERS"
          fi
        else
          echo "初回リリースです"
          RELEASE_NOTES="$RELEASE_NOTES

        ### 📝 変更履歴
        - 初回リリース"
        fi
        
        # 統計情報を追加
        if [ -n "$PREVIOUS_TAG" ]; then
          COMMIT_COUNT=$(git rev-list --count ${PREVIOUS_TAG}..HEAD)
          FILES_CHANGED=$(git diff --name-only ${PREVIOUS_TAG}..HEAD | wc -l)
          
          RELEASE_NOTES="$RELEASE_NOTES

        ### 📊 統計情報
        - コミット数: $COMMIT_COUNT
        - 変更ファイル数: $FILES_CHANGED"
        fi
        
        # インストール方法と注意事項を追加
        RELEASE_NOTES="$RELEASE_NOTES

        ### 🔧 インストール方法
        1. 上記のdmgファイルをダウンロード
        2. dmgファイルをダブルクリックしてマウント
        3. アプリケーションをApplicationsフォルダにドラッグ&ドロップ
        4. アプリケーションを起動

        ### ⚠️ 注意事項
        - 初回起動時にmacOSのセキュリティ警告が表示される場合があります
        - システム環境設定 > セキュリティとプライバシーから許可してください
        - 開発環境とプロダクション環境では異なるデータベースファイルが使用されます

        ### 🔗 関連リンク
        - [ソースコード](${{ github.server_url }}/${{ github.repository }})
        - [問題報告](${{ github.server_url }}/${{ github.repository }}/issues)
        - [ディスカッション](${{ github.server_url }}/${{ github.repository }}/discussions)

        ---

        このリリースは自動的に生成されました。  
        生成日時: $(date -Iseconds)"
        
        # リリースノートをファイルに保存（デバッグ用）
        echo "$RELEASE_NOTES" > release-notes.md
        
        # GitHub Outputに設定（改行を適切にエスケープ）
        echo "release_notes<<EOF" >> $GITHUB_OUTPUT
        echo "$RELEASE_NOTES" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        
        echo "リリースノートが生成されました"
        
    - name: GitHubリリースの作成
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      id: create-release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.version.outputs.release_tag }}
        release_name: ${{ steps.version.outputs.release_name }}
        body: ${{ steps.generate-release-notes.outputs.release_notes }}
        draft: false
        prerelease: false
        
    - name: dmgファイルのリリースアセット添付
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create-release.outputs.upload_url }}
        asset_path: ${{ steps.dmg-path.outputs.dmg_path }}
        asset_name: ${{ steps.dmg-path.outputs.dmg_name }}
        asset_content_type: application/x-apple-diskimage
        
    - name: 既存リリースへのアセット更新
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'true'
      run: |
        echo "::warning::既存のリリース ${{ steps.version.outputs.release_tag }} が存在します"
        echo "::notice::新しいビルドは成果物として保存されました: macos-dmg-${{ github.sha }}"
        echo "手動でリリースを更新する場合は、以下のファイルを使用してください:"
        echo "ファイル: ${{ steps.dmg-path.outputs.dmg_path }}"
        echo "サイズ: ${{ steps.dmg-path.outputs.dmg_size }}"
        
    - name: リリース作成成功通知
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      run: |
        echo "::notice title=リリース作成成功::GitHubリリース ${{ steps.version.outputs.release_tag }} が正常に作成されました"
        echo "::notice::リリース名: ${{ steps.version.outputs.release_name }}"
        echo "::notice::dmgファイル: ${{ steps.dmg-path.outputs.dmg_name }}"
        echo "::notice::ファイルサイズ: ${{ steps.dmg-path.outputs.dmg_size }}"
        echo "::notice::リリースURL: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.version.outputs.release_tag }}"
        
    - name: ビルド成功通知
      if: success()
      run: |
        echo "::notice title=ビルド成功::MacOSビルドが正常に完了しました"
        echo "::notice::成果物: macos-dmg-${{ github.sha }}"
        echo "::notice::コミット: ${{ github.sha }}"
        echo "::notice::ブランチ: ${{ github.ref_name }}"
        echo "ビルド完了: $(date)"
        echo "成果物がアップロードされました"
        
        # mainブランチの場合はリリース情報も表示
        if [ "${{ github.ref_name }}" = "main" ] && [ "${{ github.event_name }}" = "push" ]; then
          if [ "${{ steps.check-release.outputs.release_exists }}" = "false" ]; then
            echo "::notice::GitHubリリースが作成されました: ${{ steps.version.outputs.release_tag }}"
          else
            echo "::notice::既存リリースが存在するため、成果物のみ保存されました"
          fi
        fi
        
    # 高度な通知機能
    - name: GitHub Issues通知の作成（リリース成功時）
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          // リリース完了通知用のIssueを作成
          const issueTitle = `🎉 リリース完了通知: ${{ steps.version.outputs.release_tag }}`;
          const issueBody = `
          ## 📦 新しいリリースが完了しました
          
          **リリース情報:**
          - **リリース名**: ${{ steps.version.outputs.release_name }}
          - **タグ**: ${{ steps.version.outputs.release_tag }}
          - **dmgファイル**: ${{ steps.dmg-path.outputs.dmg_name }}
          - **ファイルサイズ**: ${{ steps.dmg-path.outputs.dmg_size }}
          
          **ダウンロードリンク:**
          [${{ steps.version.outputs.release_tag }}をダウンロード](${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.version.outputs.release_tag }})
          
          **ビルド詳細:**
          - **コミット**: ${{ github.sha }}
          - **ブランチ**: ${{ github.ref_name }}
          - **実行者**: ${{ github.actor }}
          - **実行時刻**: ${new Date().toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' })}
          - **ワークフロー実行**: [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          
          **次のステップ:**
          - [ ] リリースの動作確認
          - [ ] ユーザーへの告知
          - [ ] ドキュメントの更新（必要に応じて）
          
          ---
          
          このIssueは自動的に作成されました。確認後にクローズしてください。
          `;
          
          try {
            const issue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: issueTitle,
              body: issueBody,
              labels: ['release', 'notification', 'automated']
            });
            
            console.log(`リリース通知Issue作成完了: #${issue.data.number}`);
            
            // 関係者にメンションを追加（設定されている場合）
            const maintainers = process.env.MAINTAINERS || '';
            if (maintainers) {
              const mentionComment = `関係者の皆様へ通知: ${maintainers}\n\n新しいリリースが完了しました。ご確認をお願いします。`;
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.data.number,
                body: mentionComment
              });
            }
            
          } catch (error) {
            console.error('Issue作成エラー:', error);
            core.setFailed(`Issue作成に失敗しました: ${error.message}`);
          }
      continue-on-error: true
      env:
        MAINTAINERS: ${{ vars.MAINTAINERS || '' }}
        
    - name: GitHub Discussion通知の作成（リリース成功時）
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          // リリース告知用のDiscussionを作成
          const discussionTitle = `📢 ${{ steps.version.outputs.release_tag }} リリースのお知らせ`;
          const discussionBody = `
          ## 🎉 新バージョンがリリースされました！
          
          **${{ steps.version.outputs.release_name }}** が正式にリリースされました。
          
          ### 📦 ダウンロード
          
          **MacOS用アプリケーション:**
          [${{ steps.dmg-path.outputs.dmg_name }}をダウンロード](${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.version.outputs.release_tag }})
          
          **ファイルサイズ:** ${{ steps.dmg-path.outputs.dmg_size }}
          
          ### 🔧 インストール方法
          
          1. 上記のdmgファイルをダウンロード
          2. dmgファイルをダブルクリックしてマウント
          3. アプリケーションをApplicationsフォルダにドラッグ&ドロップ
          4. アプリケーションを起動
          
          ### ⚠️ 注意事項
          
          - 初回起動時にmacOSのセキュリティ警告が表示される場合があります
          - システム環境設定 > セキュリティとプライバシーから許可してください
          - 開発環境とプロダクション環境では異なるデータベースファイルが使用されます
          
          ### 🐛 問題報告
          
          何か問題が発生した場合は、[Issues](${{ github.server_url }}/${{ github.repository }}/issues)でお知らせください。
          
          ### 💬 フィードバック
          
          このリリースについてのご意見・ご感想をお聞かせください！
          
          ---
          
          リリース日時: ${new Date().toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' })}  
          コミット: ${{ github.sha }}
          `;
          
          try {
            // GraphQL APIを使用してDiscussionを作成
            const query = \`
              mutation CreateDiscussion($repositoryId: ID!, $categoryId: ID!, $title: String!, $body: String!) {
                createDiscussion(input: {
                  repositoryId: $repositoryId,
                  categoryId: $categoryId,
                  title: $title,
                  body: $body
                }) {
                  discussion {
                    id
                    number
                    url
                  }
                }
              }
            \`;
            
            // リポジトリIDを取得
            const repoQuery = \`
              query GetRepository($owner: String!, $name: String!) {
                repository(owner: $owner, name: $name) {
                  id
                  discussionCategories(first: 10) {
                    nodes {
                      id
                      name
                    }
                  }
                }
              }
            \`;
            
            const repoData = await github.graphql(repoQuery, {
              owner: context.repo.owner,
              name: context.repo.repo
            });
            
            // "Announcements"カテゴリを探す（なければ最初のカテゴリを使用）
            const categories = repoData.repository.discussionCategories.nodes;
            const announcementCategory = categories.find(cat => 
              cat.name.toLowerCase().includes('announcement') || 
              cat.name.toLowerCase().includes('お知らせ') ||
              cat.name.toLowerCase().includes('release')
            ) || categories[0];
            
            if (!announcementCategory) {
              console.log('Discussionカテゴリが見つかりません。Discussionの作成をスキップします。');
              return;
            }
            
            const discussionData = await github.graphql(query, {
              repositoryId: repoData.repository.id,
              categoryId: announcementCategory.id,
              title: discussionTitle,
              body: discussionBody
            });
            
            console.log(\`リリース告知Discussion作成完了: #\${discussionData.createDiscussion.discussion.number}\`);
            console.log(\`URL: \${discussionData.createDiscussion.discussion.url}\`);
            
          } catch (error) {
            console.error('Discussion作成エラー:', error);
            // Discussionが無効な場合はスキップ（エラーにしない）
            console.log('Discussionの作成をスキップしました');
          }
      continue-on-error: true
      
    - name: Slack通知の送信
      if: always() && (success() || failure())
      uses: 8398a7/action-slack@v3
      with:
        status: ${{ job.status }}
        channel: '#ci-cd'
        text: |
          MacOSビルド ${{ job.status == 'success' && '成功' || '失敗' }}
          コミット: ${{ github.sha }}
          ブランチ: ${{ github.ref_name }}
          実行者: ${{ github.actor }}
          ${{ job.status == 'success' && '成果物がダウンロード可能です' || 'ログを確認してください' }}
          ${{ job.status == 'success' && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false' && format('🎉 新しいリリースが作成されました: {0}', steps.version.outputs.release_tag) || '' }}
        webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
      continue-on-error: true  # 通知失敗でもビルドは成功扱い
      
    - name: メール通知の送信（失敗時）
      if: failure() && github.ref_name == 'main'
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: '[緊急] MacOSビルド失敗 - ${{ github.repository }}'
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: 'GitHub Actions <noreply@github.com>'
        body: |
          MacOSビルドプロセスが失敗しました。
          
          リポジトリ: ${{ github.repository }}
          ブランチ: ${{ github.ref_name }}
          コミット: ${{ github.sha }}
          実行者: ${{ github.actor }}
          実行時刻: ${{ github.event.head_commit.timestamp }}
          
          詳細ログ: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          
          至急確認をお願いします。
      continue-on-error: true
      
    - name: メール通知の送信（リリース成功時）
      if: success() && github.ref_name == 'main' && github.event_name == 'push' && steps.check-release.outputs.release_exists == 'false'
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 587
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: '🎉 新しいリリース作成完了 - ${{ steps.version.outputs.release_tag }}'
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: 'GitHub Actions <noreply@github.com>'
        body: |
          新しいMacOSリリースが正常に作成されました。
          
          📦 リリース情報:
          - リリース名: ${{ steps.version.outputs.release_name }}
          - タグ: ${{ steps.version.outputs.release_tag }}
          - dmgファイル: ${{ steps.dmg-path.outputs.dmg_name }}
          - ファイルサイズ: ${{ steps.dmg-path.outputs.dmg_size }}
          
          🔗 ダウンロード:
          ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.version.outputs.release_tag }}
          
          📋 ビルド情報:
          - リポジトリ: ${{ github.repository }}
          - ブランチ: ${{ github.ref_name }}
          - コミット: ${{ github.sha }}
          - 実行者: ${{ github.actor }}
          - 実行時刻: ${{ github.event.head_commit.timestamp }}
          
          リリースが正常に完了しました。
      continue-on-error: true
      
    # 通知統合とフォールバック機能
    - name: 通知状況の確認と統合レポート
      if: always() && github.ref_name == 'main' && github.event_name == 'push'
      run: |
        echo "=== 通知機能実行結果レポート ==="
        echo "実行日時: $(date -Iseconds)"
        echo "リポジトリ: ${{ github.repository }}"
        echo "コミット: ${{ github.sha }}"
        echo "ブランチ: ${{ github.ref_name }}"
        echo "実行者: ${{ github.actor }}"
        echo ""
        
        # ビルド結果の確認
        if [ "${{ job.status }}" = "success" ]; then
          echo "✅ ビルド状況: 成功"
          
          # リリース作成状況の確認
          if [ "${{ steps.check-release.outputs.release_exists }}" = "false" ]; then
            echo "✅ リリース作成: 成功"
            echo "   - リリースタグ: ${{ steps.version.outputs.release_tag }}"
            echo "   - リリース名: ${{ steps.version.outputs.release_name }}"
            echo "   - dmgファイル: ${{ steps.dmg-path.outputs.dmg_name }}"
            echo "   - ファイルサイズ: ${{ steps.dmg-path.outputs.dmg_size }}"
            echo "   - リリースURL: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.version.outputs.release_tag }}"
            
            # 通知機能の実行状況を記録
            echo ""
            echo "📢 実行された通知機能:"
            echo "   - GitHub Issues通知: 実行済み"
            echo "   - GitHub Discussion通知: 実行済み"
            echo "   - Slack通知: 実行済み（設定されている場合）"
            echo "   - メール通知: 実行済み（設定されている場合）"
            echo "   - GitHub Actions通知: 実行済み"
            
            # 関係者への通知確認
            echo ""
            echo "👥 関係者通知状況:"
            if [ -n "${{ vars.MAINTAINERS }}" ]; then
              echo "   - メンテナー: ${{ vars.MAINTAINERS }}"
              echo "   - 通知方法: GitHub Issues, Discussion"
            else
              echo "   - メンテナー: 未設定（MAINTAINERS変数を設定してください）"
            fi
            
            if [ -n "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
              echo "   - Slack通知: 設定済み"
            else
              echo "   - Slack通知: 未設定（SLACK_WEBHOOK_URL秘密を設定してください）"
            fi
            
            if [ -n "${{ secrets.NOTIFICATION_EMAIL }}" ]; then
              echo "   - メール通知: 設定済み"
            else
              echo "   - メール通知: 未設定（NOTIFICATION_EMAIL秘密を設定してください）"
            fi
            
          else
            echo "ℹ️  リリース作成: 既存リリース存在のためスキップ"
            echo "   - 既存タグ: ${{ steps.version.outputs.release_tag }}"
            echo "   - 成果物: macos-dmg-${{ github.sha }} として保存済み"
          fi
          
        else
          echo "❌ ビルド状況: 失敗"
          echo "   - エラーログ: build-logs-${{ github.sha }} を確認してください"
          echo "   - 実行ログ: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          # 失敗時の通知状況
          echo ""
          echo "📢 失敗通知機能:"
          echo "   - GitHub Actions通知: 実行済み"
          echo "   - Slack通知: 実行済み（設定されている場合）"
          echo "   - メール通知: 実行済み（設定されている場合）"
        fi
        
        echo ""
        echo "=== 通知設定の推奨事項 ==="
        echo "より効果的な通知のために、以下の設定を検討してください:"
        echo ""
        echo "1. リポジトリ変数の設定:"
        echo "   - MAINTAINERS: 関係者のGitHubユーザー名（例: @user1 @user2）"
        echo ""
        echo "2. リポジトリ秘密の設定（オプション）:"
        echo "   - SLACK_WEBHOOK_URL: Slack Incoming Webhook URL"
        echo "   - NOTIFICATION_EMAIL: 通知先メールアドレス"
        echo "   - MAIL_USERNAME: SMTP認証用ユーザー名"
        echo "   - MAIL_PASSWORD: SMTP認証用パスワード"
        echo ""
        echo "3. GitHub機能の活用:"
        echo "   - Issues: 自動作成される通知Issueを確認"
        echo "   - Discussions: リリース告知の確認"
        echo "   - Releases: 自動作成されるリリースページの確認"
        echo ""
        echo "=== レポート終了 ==="
        
    # 最終的な通知確認とフォールバック
    - name: 最終通知確認とフォールバック
      if: always()
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          // 最終的な通知状況を確認し、必要に応じてフォールバック通知を実行
          const jobStatus = '${{ job.status }}';
          const isMainBranch = '${{ github.ref_name }}' === 'main';
          const isPush = '${{ github.event_name }}' === 'push';
          const releaseCreated = '${{ steps.check-release.outputs.release_exists }}' === 'false';
          
          console.log('=== 最終通知確認 ===');
          console.log(`ジョブ状況: ${jobStatus}`);
          console.log(`mainブランチ: ${isMainBranch}`);
          console.log(`プッシュイベント: ${isPush}`);
          console.log(`リリース作成: ${releaseCreated}`);
          
          // 成功時の最終確認
          if (jobStatus === 'success') {
            console.log('✅ ビルドプロセスが正常に完了しました');
            
            if (isMainBranch && isPush) {
              if (releaseCreated) {
                console.log('🎉 新しいリリースが作成され、すべての通知が実行されました');
                
                // 成功の最終通知をGitHub Actionsログに記録
                core.notice('リリース完了通知', {
                  title: 'リリース作成成功',
                  file: '.github/workflows/release.yml'
                });
                
                core.notice(`新しいリリース ${{ steps.version.outputs.release_tag }} が正常に作成されました`, {
                  title: 'リリース情報'
                });
                
                core.notice(`ダウンロード: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ steps.version.outputs.release_tag }}`, {
                  title: 'ダウンロードリンク'
                });
                
              } else {
                console.log('ℹ️ 既存リリースが存在するため、成果物のみ保存されました');
                
                core.notice('既存リリース検出', {
                  title: 'リリース状況',
                  file: '.github/workflows/release.yml'
                });
              }
            } else {
              console.log('ℹ️ 開発ブランチでのビルドが完了しました');
            }
            
          } else {
            console.log('❌ ビルドプロセスが失敗しました');
            
            // 失敗時の緊急通知
            core.error('ビルド失敗', {
              title: 'CI/CDエラー',
              file: '.github/workflows/release.yml'
            });
            
            if (isMainBranch) {
              core.error('mainブランチでビルドが失敗しました。至急確認が必要です。', {
                title: '緊急: mainブランチビルド失敗'
              });
            }
          }
          
          // 通知機能の要件充足確認
          console.log('');
          console.log('=== 要件5.4の充足確認 ===');
          console.log('要件: リリースプロセスが完了する THEN システムは関係者に通知を送信する');
          console.log('');
          
          if (jobStatus === 'success' && isMainBranch && isPush && releaseCreated) {
            console.log('✅ 実装された通知機能:');
            console.log('   1. GitHub Issues通知 - リリース完了Issue自動作成');
            console.log('   2. GitHub Discussion通知 - リリース告知Discussion作成');
            console.log('   3. GitHub Actions通知 - ワークフローログでの通知');
            console.log('   4. Slack通知 - 設定されている場合に送信');
            console.log('   5. メール通知 - 設定されている場合に送信');
            console.log('   6. 関係者メンション - MAINTAINERS変数設定時');
            console.log('');
            console.log('✅ 要件5.4は正常に満たされています');
          } else {
            console.log('ℹ️ リリース作成条件を満たしていないため、通知は限定的です');
          }
          
          console.log('=== 最終確認完了 ===');