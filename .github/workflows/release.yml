name: クロスプラットフォームリリース

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
      - 'LICENSE'
  # pull_request:
  #   branches: [ main ]
  #   paths-ignore:
  #     - '**.md'
  #     - 'docs/**'
  #     - '.gitignore'
  #     - 'LICENSE'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # バージョンとタグの生成
  version-tag:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.version.outputs.VERSION }}
      RELEASE_TAG: ${{ steps.version.outputs.RELEASE_TAG }}
      COMMIT_SHORT: ${{ steps.version.outputs.COMMIT_SHORT }}
      TIMESTAMP: ${{ steps.version.outputs.TIMESTAMP }}
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: バージョン情報の生成
      id: version
      run: |
        # package.jsonからバージョンを取得
        VERSION=$(node -p "require('./package.json').version")
        
        # 日本時間でのタイムスタンプ生成
        TIMESTAMP=$(TZ=Asia/Tokyo date '+%Y%m%d-%H%M%S')
        
        # リリースタグの生成
        RELEASE_TAG="v${VERSION}-${TIMESTAMP}"
        
        # コミットハッシュ（短縮版）
        COMMIT_SHORT=$(git rev-parse --short HEAD)
        
        echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
        echo "RELEASE_TAG=${RELEASE_TAG}" >> $GITHUB_OUTPUT
        echo "COMMIT_SHORT=${COMMIT_SHORT}" >> $GITHUB_OUTPUT
        echo "TIMESTAMP=${TIMESTAMP}" >> $GITHUB_OUTPUT
        
        echo "📋 生成されたバージョン情報:"
        echo "  - バージョン: ${VERSION}"
        echo "  - リリースタグ: ${RELEASE_TAG}"
        echo "  - コミット: ${COMMIT_SHORT}"
        echo "  - タイムスタンプ: ${TIMESTAMP}"

  # MacOSビルド
  build-macos:
    needs: version-tag
    runs-on: macos-latest
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
    
    steps:
    - uses: actions/checkout@v4
    
    - name: pnpmのインストール
      uses: pnpm/action-setup@v4
      with:
        version: 10
    
    - name: Node.js環境のセットアップ
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'pnpm'
    
    - name: Rust環境のセットアップ
      uses: dtolnay/rust-toolchain@stable
    
    - name: Apple Developer証明書のインポート
      env:
        APPLE_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
        APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
        KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
      run: |
        echo "🔐 Apple Developer証明書をインポート中..."
        
        # 証明書をデコードして一時ファイルに保存
        echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12
        
        # 新しいキーチェーンを作成
        security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
        security default-keychain -s build.keychain
        security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
        security set-keychain-settings -t 3600 -u build.keychain
        
        # 証明書をキーチェーンにインポート
        security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
        security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
        
        # インポートされた証明書を確認
        security find-identity -v -p codesigning build.keychain
        
        # 一時ファイルを削除
        rm certificate.p12
        
        echo "✅ Apple Developer証明書のインポートが完了しました"
    
    - name: 証明書の検証と署名IDの設定
      run: |
        echo "🔍 証明書情報を確認中..."
        
        # 証明書情報を取得
        CERT_INFO=$(security find-identity -v -p codesigning build.keychain | grep "Apple Development\|Developer ID Application")
        
        if [ -z "$CERT_INFO" ]; then
          echo "❌ 有効な証明書が見つかりません"
          exit 1
        fi
        
        # 証明書IDを抽出
        CERT_ID=$(echo "$CERT_INFO" | head -n 1 | awk -F'"' '{print $2}')
        echo "CERT_ID=$CERT_ID" >> $GITHUB_ENV
        
        echo "✅ 証明書が確認されました: $CERT_ID"
    
    - name: 依存関係のインストール
      run: pnpm install
    
    - name: フロントエンドビルド
      run: pnpm build
    
    - name: MacOSアプリケーションのビルド
      run: pnpm tauri build
      env:
        # Apple署名関連（動的に取得した証明書IDを使用）
        APPLE_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
        APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
        APPLE_SIGNING_IDENTITY: ${{ env.CERT_ID }}
        APPLE_ID: ${{ secrets.APPLE_ID }}
        APPLE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
        APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        # アプリケーション環境設定
        ENVIRONMENT: production
        DEBUG: false
        LOG_LEVEL: info
        # Cloudflare R2設定（本番環境用）
        R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
        R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
        R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}

    
    - name: ビルド結果の確認
      run: |
        echo "🎯 MacOSビルド完了"
        echo "📦 生成されたファイル:"
        find src-tauri/target/release/bundle/dmg -name "*.dmg" -exec ls -lh {} \; || echo "dmgファイルが見つかりません"
        
        if [ "${{ github.event_name }}" = "pull_request" ]; then
          echo "✅ PR用ビルドテスト完了 - リリースは作成されません"
        else
          echo "🚀 mainブランチへのpush - リリース作成準備完了"
        fi
    
    - name: MacOS成果物のアップロード
      uses: actions/upload-artifact@v4
      with:
        name: macos-artifacts
        path: src-tauri/target/release/bundle/dmg/*.dmg
        retention-days: 7
    
    - name: MacOS署名環境のクリーンアップ
      if: always()
      run: |
        echo "🧹 MacOS署名環境をクリーンアップ中..."
        
        # キーチェーンを削除
        security delete-keychain build.keychain 2>/dev/null || true
        
        # 環境変数をクリア
        unset APPLE_CERTIFICATE
        unset APPLE_CERTIFICATE_PASSWORD
        unset APPLE_ID
        unset APPLE_ID_PASSWORD
        unset APPLE_TEAM_ID
        
        echo "✅ MacOS署名環境のクリーンアップが完了しました"

  # Windowsビルド
  build-windows:
    needs: version-tag
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: pnpmのインストール
      uses: pnpm/action-setup@v4
      with:
        version: 10
    
    - name: Node.js環境のセットアップ
      uses: actions/setup-node@v4
      with:
        node-version: '24'
        cache: 'pnpm'
    
    - name: Rust環境のセットアップ
      uses: dtolnay/rust-toolchain@stable
    
    - name: 依存関係のインストール
      run: pnpm install
    
    - name: フロントエンドビルド
      run: pnpm build
    
    - name: Windowsアプリケーションのビルド
      run: pnpm tauri build
      env:
        # アプリケーション環境設定
        ENVIRONMENT: production
        DEBUG: false
        LOG_LEVEL: info
        # Cloudflare R2設定（本番環境用）
        R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
        R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
        R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
    
    - name: ビルド結果の確認
      run: |
        echo "🎯 Windowsビルド完了"
        echo "📦 生成されたファイル:"
        Get-ChildItem -Path "src-tauri/target/release/bundle/msi" -Filter "*.msi" | ForEach-Object { 
          Write-Host "  - $($_.Name) ($([math]::Round($_.Length/1MB, 2)) MB)"
        }
        
        if ("${{ github.event_name }}" -eq "pull_request") {
          echo "✅ PR用ビルドテスト完了 - リリースは作成されません"
        } else {
          echo "🚀 mainブランチへのpush - リリース作成準備完了"
        }
      shell: pwsh
    
    - name: Windows成果物のアップロード
      uses: actions/upload-artifact@v4
      with:
        name: windows-artifacts
        path: src-tauri/target/release/bundle/msi/*.msi
        retention-days: 7

  # リリース作成
  create-release:
    needs: [version-tag, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: 成果物のダウンロード
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts
    
    - name: リリースの作成とアップロード
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const fs = require('fs');
          const path = require('path');
          
          const version = '${{ needs.version-tag.outputs.VERSION }}';
          const releaseTag = '${{ needs.version-tag.outputs.RELEASE_TAG }}';
          const commitShort = '${{ needs.version-tag.outputs.COMMIT_SHORT }}';
          
          console.log('🚀 リリース作成開始: ' + releaseTag);
          
          // 成果物ファイルの検索
          const dmgFiles = fs.readdirSync('./artifacts/macos-artifacts').filter(f => f.endsWith('.dmg'));
          const msiFiles = fs.readdirSync('./artifacts/windows-artifacts').filter(f => f.endsWith('.msi'));
          
          if (dmgFiles.length === 0 || msiFiles.length === 0) {
            throw new Error('必要な成果物ファイルが見つかりません');
          }
          
          const dmgPath = path.join('./artifacts/macos-artifacts', dmgFiles[0]);
          const msiPath = path.join('./artifacts/windows-artifacts', msiFiles[0]);
          
          // ファイルサイズの取得
          const dmgStats = fs.statSync(dmgPath);
          const msiStats = fs.statSync(msiPath);
          const dmgSizeMB = (dmgStats.size / 1024 / 1024).toFixed(2);
          const msiSizeMB = (msiStats.size / 1024 / 1024).toFixed(2);
          
          console.log('📦 成果物ファイル:');
          console.log('  - MacOS: ' + dmgFiles[0] + ' (' + dmgSizeMB + ' MB)');
          console.log('  - Windows: ' + msiFiles[0] + ' (' + msiSizeMB + ' MB)');
          
          // リリースノートの生成
          const releaseNotes = [
            '## 🎉 新しいクロスプラットフォームリリース: ' + releaseTag,
            '',
            '### 📦 ダウンロード',
            '| プラットフォーム | ファイル | サイズ |',
            '|---|---|---|',
            '| **MacOS** | ' + dmgFiles[0] + ' | ' + dmgSizeMB + ' MB |',
            '| **Windows** | ' + msiFiles[0] + ' | ' + msiSizeMB + ' MB |',
            '',
            '### 📋 ビルド情報',
            '- **バージョン**: ' + version,
            '- **リリースタグ**: ' + releaseTag,
            '- **コミット**: ' + context.sha + ' (' + commitShort + ')',
            '- **ビルド日時**: ' + new Date().toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' }) + ' (JST)',
            '- **ビルド者**: ' + context.actor,
            '',
            '### 🔧 インストール方法',
            '',
            '#### MacOS',
            '1. dmgファイルをダウンロード',
            '2. dmgファイルをダブルクリックしてマウント',
            '3. アプリケーションをApplicationsフォルダにドラッグ&ドロップ',
            '',
            '#### Windows',
            '1. msiファイルをダウンロード',
            '2. msiファイルを右クリックして「管理者として実行」を選択',
            '3. インストールウィザードに従ってインストール'
          ].join('\n');
          
          // リリースの作成
          console.log('📝 GitHubリリースを作成中...');
          const release = await github.rest.repos.createRelease({
            owner: context.repo.owner,
            repo: context.repo.repo,
            tag_name: releaseTag,
            name: 'オラの経費だゾ ' + releaseTag,
            body: releaseNotes,
            draft: false,
            prerelease: false
          });
          
          console.log('✅ リリースが作成されました: ' + release.data.html_url);
          
          // dmgファイルのアップロード
          console.log('📤 MacOS dmgファイルをアップロード中...');
          const dmgContent = fs.readFileSync(dmgPath);
          await github.rest.repos.uploadReleaseAsset({
            owner: context.repo.owner,
            repo: context.repo.repo,
            release_id: release.data.id,
            name: dmgFiles[0],
            data: dmgContent
          });
          
          // msiファイルのアップロード
          console.log('📤 Windows msiファイルをアップロード中...');
          const msiContent = fs.readFileSync(msiPath);
          await github.rest.repos.uploadReleaseAsset({
            owner: context.repo.owner,
            repo: context.repo.repo,
            release_id: release.data.id,
            name: msiFiles[0],
            data: msiContent
          });
          
          console.log('🎉 クロスプラットフォームリリースが正常に作成されました！');
          console.log('🔗 リリースURL: ' + release.data.html_url);


  # ビルド結果のサマリー（PR用）
  build-summary:
    needs: [version-tag, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: ビルド結果のサマリー表示
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const version = '${{ needs.version-tag.outputs.VERSION }}';
          const releaseTag = '${{ needs.version-tag.outputs.RELEASE_TAG }}';
          const macosResult = '${{ needs.build-macos.result }}';
          const windowsResult = '${{ needs.build-windows.result }}';
          const eventName = '${{ github.event_name }}';
          
          console.log('='.repeat(60));
          console.log('🎯 クロスプラットフォームビルド結果サマリー');
          console.log('='.repeat(60));
          console.log('');
          console.log('📋 ビルド情報:');
          console.log('  - バージョン: ' + version);
          console.log('  - リリースタグ: ' + releaseTag);
          console.log('  - イベント: ' + eventName);
          console.log('');
          console.log('📊 ビルド結果:');
          console.log('  - MacOS: ' + (macosResult === 'success' ? '✅ 成功' : '❌ 失敗'));
          console.log('  - Windows: ' + (windowsResult === 'success' ? '✅ 成功' : '❌ 失敗'));
          console.log('');
          
          if (eventName === 'pull_request') {
            console.log('📝 PR用ビルドテスト:');
            console.log('  ✅ ビルドテストが完了しました');
            console.log('  ℹ️  リリースは作成されません（PR用テスト）');
            console.log('  💡 mainブランチにマージ後、自動的にリリースが作成されます');
          } else if (macosResult === 'success' && windowsResult === 'success') {
            console.log('🚀 リリース作成:');
            console.log('  ✅ すべてのビルドが成功しました');
            console.log('  📦 リリース作成ジョブが実行されます');
          } else {
            console.log('⚠️  ビルド失敗:');
            console.log('  ❌ 一部のビルドが失敗したため、リリースは作成されません');
          }
          
          console.log('');
          console.log('='.repeat(60));
