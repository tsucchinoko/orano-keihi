name: クロスプラットフォームリリース

on:
  push:
    branches:
      - release
      - "release/**"
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".gitignore"
      - "LICENSE"
  pull_request:
    branches:
      - release
      - "release/**"
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".gitignore"
      - "LICENSE"

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # バージョンとタグの生成
  version-tag:
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.version.outputs.VERSION }}
      RELEASE_TAG: ${{ steps.version.outputs.RELEASE_TAG }}
      COMMIT_SHORT: ${{ steps.version.outputs.COMMIT_SHORT }}
      TIMESTAMP: ${{ steps.version.outputs.TIMESTAMP }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: バージョン情報の生成
        id: version
        run: |
          # package.jsonからバージョンを取得
          VERSION=$(node -p "require('./packages/desktop/package.json').version")

          # 日本時間でのタイムスタンプ生成
          TIMESTAMP=$(TZ=Asia/Tokyo date '+%Y%m%d-%H%M%S')

          # リリースタグの生成
          RELEASE_TAG="v${VERSION}-${TIMESTAMP}"

          # コミットハッシュ（短縮版）
          COMMIT_SHORT=$(git rev-parse --short HEAD)

          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          echo "RELEASE_TAG=${RELEASE_TAG}" >> $GITHUB_OUTPUT
          echo "COMMIT_SHORT=${COMMIT_SHORT}" >> $GITHUB_OUTPUT
          echo "TIMESTAMP=${TIMESTAMP}" >> $GITHUB_OUTPUT

          echo "📋 生成されたバージョン情報:"
          echo "  - バージョン: ${VERSION}"
          echo "  - リリースタグ: ${RELEASE_TAG}"
          echo "  - コミット: ${COMMIT_SHORT}"
          echo "  - タイムスタンプ: ${TIMESTAMP}"

  # MacOSビルド
  build-macos:
    needs: version-tag
    runs-on: macos-latest
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}

    steps:
      - uses: actions/checkout@v4

      - name: pnpmのインストール
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Node.js環境のセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "pnpm"

      - name: Rust環境のセットアップ
        uses: dtolnay/rust-toolchain@stable

      - name: Apple Developer証明書のインポート
        env:
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          echo "🔐 Apple Developer証明書をインポート中..."

          # 証明書をデコードして一時ファイルに保存
          echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12

          # 新しいキーチェーンを作成
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

          # 証明書をキーチェーンにインポート
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain

          # インポートされた証明書を確認
          security find-identity -v -p codesigning build.keychain

          # 一時ファイルを削除
          rm certificate.p12

          echo "✅ Apple Developer証明書のインポートが完了しました"

      - name: 証明書の検証と署名IDの設定
        run: |
          echo "🔍 証明書情報を確認中..."

          # 証明書情報を取得
          CERT_INFO=$(security find-identity -v -p codesigning build.keychain | grep "Apple Development\|Developer ID Application")

          if [ -z "$CERT_INFO" ]; then
            echo "❌ 有効な証明書が見つかりません"
            exit 1
          fi

          # 証明書IDを抽出
          CERT_ID=$(echo "$CERT_INFO" | head -n 1 | awk -F'"' '{print $2}')
          echo "APPLE_SIGNING_IDENTITY=$CERT_ID" >> $GITHUB_ENV

          echo "✅ 証明書が確認されました: $CERT_ID"

      - name: 依存関係のインストール
        run: pnpm install

      - name: フロントエンドビルド
        run: pnpm build

      - name: MacOSアプリケーションのビルド
        run: pnpm tauri build
        env:
          # Apple署名関連
          APPLE_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          APPLE_SIGNING_IDENTITY: ${{ env.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Tauri署名関連
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # アプリケーション環境設定
          ENVIRONMENT: production
          DEBUG: false
          LOG_LEVEL: info
          # APIサーバー設定（本番環境）
          API_SERVER_URL: https://orano-keihi.tsucchinoko.workers.dev
          API_TIMEOUT_SECONDS: 30
          API_MAX_RETRIES: 3
          # Google OAuth 2.0設定
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REDIRECT_URI: http://127.0.0.1/callback
          # セッション暗号化キー
          SESSION_ENCRYPTION_KEY: ${{ secrets.SESSION_ENCRYPTION_KEY }}

      - name: Tauri署名ファイルの生成
        run: |
          echo "🔐 Tauri署名ファイルを生成中..."

          # DMGファイルを検索（モノレポ構造に対応）
          DMG_FILE=$(find . -type f -name "*.dmg" -path "*/target/release/bundle/dmg/*" | head -n 1)

          if [ -z "$DMG_FILE" ]; then
            echo "❌ DMGファイルが見つかりません"
            exit 1
          fi

          # 絶対パスに変換
          DMG_FILE_ABS=$(realpath "$DMG_FILE")
          echo "📦 署名対象ファイル: $DMG_FILE_ABS"

          # 秘密鍵を一時ファイルに保存（改行を削除）
          TEMP_KEY_FILE=$(mktemp)
          echo "$TAURI_SIGNING_PRIVATE_KEY" | tr -d '\n' > "$TEMP_KEY_FILE"
          chmod 600 "$TEMP_KEY_FILE"

          # Tauri署名を生成
          cd packages/desktop
          if [ -n "$TAURI_SIGNING_PRIVATE_KEY_PASSWORD" ]; then
            pnpm tauri signer sign "$DMG_FILE_ABS" --private-key-path "$TEMP_KEY_FILE" -p "$TAURI_SIGNING_PRIVATE_KEY_PASSWORD"
          else
            pnpm tauri signer sign "$DMG_FILE_ABS" --private-key-path "$TEMP_KEY_FILE"
          fi
          cd ../..

          # 一時ファイルを削除
          rm -f "$TEMP_KEY_FILE"

          # 署名ファイルの確認
          SIG_FILE="${DMG_FILE_ABS}.sig"
          if [ -f "$SIG_FILE" ]; then
            echo "✅ 署名ファイルが生成されました: $SIG_FILE"
            echo "📋 署名内容:"
            cat "$SIG_FILE"
          else
            echo "❌ 署名ファイルの生成に失敗しました"
            exit 1
          fi
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: ビルド結果の確認
        run: |
          echo "🎯 MacOSビルド完了"
          echo "📦 生成されたファイル:"

          # dmgファイルを再帰的に検索
          find . -type f -name "*.dmg" -path "*/target/release/bundle/dmg/*" -exec ls -lh {} \; 2>/dev/null || {
            echo "⚠️ dmgファイルが見つかりません"
            echo "ディレクトリ構造を確認中..."
            find . -type d -name "bundle" -path "*/target/release/*" 2>/dev/null | while read dir; do
              echo "  - $dir"
              ls -la "$dir" 2>/dev/null || true
            done
          }

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "✅ PR用ビルドテスト完了 - リリースは作成されません"
          else
            echo "🚀 mainブランチへのpush - リリース作成準備完了"
          fi

      - name: MacOS成果物のアップロード
        uses: actions/upload-artifact@v4
        with:
          name: macos-artifacts
          path: |
            packages/desktop/src-tauri/target/release/bundle/dmg/*.dmg
            packages/desktop/src-tauri/target/release/bundle/dmg/*.dmg.sig
          retention-days: 7

      - name: MacOS署名環境のクリーンアップ
        if: always()
        run: |
          echo "🧹 MacOS署名環境をクリーンアップ中..."

          # キーチェーンを削除
          security delete-keychain build.keychain 2>/dev/null || true

          # 環境変数をクリア
          unset APPLE_CERTIFICATE
          unset APPLE_CERTIFICATE_PASSWORD
          unset APPLE_ID
          unset APPLE_ID_PASSWORD
          unset APPLE_TEAM_ID

          echo "✅ MacOS署名環境のクリーンアップが完了しました"

  # Windowsビルド
  build-windows:
    needs: version-tag
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: pnpmのインストール
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Node.js環境のセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "pnpm"

      - name: Rust環境のセットアップ
        uses: dtolnay/rust-toolchain@stable

      - name: 依存関係のインストール
        run: pnpm install

      - name: フロントエンドビルド
        run: pnpm build

      - name: Windowsアプリケーションのビルド
        run: pnpm tauri build
        env:
          # Tauri署名関連
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # アプリケーション環境設定
          ENVIRONMENT: production
          DEBUG: false
          LOG_LEVEL: info
          # APIサーバー設定（本番環境）
          API_SERVER_URL: https://orano-keihi.tsucchinoko.workers.dev
          API_TIMEOUT_SECONDS: 30
          API_MAX_RETRIES: 3
          # Google OAuth 2.0設定
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REDIRECT_URI: http://127.0.0.1/callback
          # セッション暗号化キー
          SESSION_ENCRYPTION_KEY: ${{ secrets.SESSION_ENCRYPTION_KEY }}
          # Windows署名設定
          WINDOWS_TIMESTAMP_URL: http://timestamp.digicert.com

      - name: Tauri署名ファイルの生成（Windows）
        run: |
          Write-Host "🔐 Windows用Tauri署名ファイルを生成中..."

          # MSIファイルを検索（モノレポ構造に対応）
          $msiFiles = Get-ChildItem -Path . -Filter "*.msi" -Recurse -ErrorAction SilentlyContinue | Where-Object { $_.FullName -like "*target*release*bundle*msi*" }

          if ($msiFiles.Count -eq 0) {
            Write-Host "❌ MSIファイルが見つかりません"
            exit 1
          }

          $msiFile = $msiFiles[0].FullName
          Write-Host "📦 署名対象ファイル: $msiFile"

          # 秘密鍵を一時ファイルに保存（改行を削除）
          $tempKeyFile = [System.IO.Path]::GetTempFileName()
          $env:TAURI_SIGNING_PRIVATE_KEY -replace "`n", "" -replace "`r", "" | Out-File -FilePath $tempKeyFile -Encoding ASCII -NoNewline

          try {
            # Tauri署名を生成
            Set-Location "packages/desktop"
            
            if ($env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD) {
              pnpm tauri signer sign "$msiFile" --private-key-path "$tempKeyFile" -p "$env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD"
            } else {
              pnpm tauri signer sign "$msiFile" --private-key-path "$tempKeyFile"
            }
            
            Set-Location "../.."

            # 署名ファイルの確認
            $sigFile = "$msiFile.sig"
            if (Test-Path $sigFile) {
              Write-Host "✅ 署名ファイルが生成されました: $sigFile"
              Write-Host "📋 署名内容:"
              Get-Content $sigFile
            } else {
              Write-Host "❌ 署名ファイルの生成に失敗しました"
              exit 1
            }
          } finally {
            # 一時ファイルを削除
            if (Test-Path $tempKeyFile) {
              Remove-Item $tempKeyFile -Force
            }
          }
        shell: pwsh
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: ビルド結果の確認
        run: |
          echo "🎯 Windowsビルド完了"
          echo "📦 生成されたファイル:"

          # msiファイルを再帰的に検索
          $msiFiles = Get-ChildItem -Path . -Filter "*.msi" -Recurse -ErrorAction SilentlyContinue | Where-Object { $_.FullName -like "*target*release*bundle*msi*" }

          if ($msiFiles) {
            foreach ($file in $msiFiles) {
              Write-Host "  - $($file.Name) ($([math]::Round($file.Length/1MB, 2)) MB)"
              Write-Host "    場所: $($file.DirectoryName)"
            }
          } else {
            Write-Host "⚠️ msiファイルが見つかりません"
            Write-Host "ディレクトリ構造を確認中..."
            Get-ChildItem -Path . -Directory -Recurse -Filter "bundle" -ErrorAction SilentlyContinue | ForEach-Object {
              Write-Host "  - $($_.FullName)"
              Get-ChildItem -Path $_.FullName -ErrorAction SilentlyContinue | ForEach-Object {
                Write-Host "    - $($_.Name)"
              }
            }
          }

          if ("${{ github.event_name }}" -eq "pull_request") {
            echo "✅ PR用ビルドテスト完了 - リリースは作成されません"
          } else {
            echo "🚀 mainブランチへのpush - リリース作成準備完了"
          }
        shell: pwsh

      - name: Windows成果物のアップロード
        uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: |
            packages/desktop/src-tauri/target/release/bundle/msi/*.msi
            packages/desktop/src-tauri/target/release/bundle/msi/*.msi.sig
          retention-days: 7

  # リリース作成
  create-release:
    needs: [version-tag, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: success() && github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release')

    steps:
      - uses: actions/checkout@v4

      - name: Node.js環境のセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: 成果物のダウンロード
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: ダウンロードされた成果物の確認
        run: |
          echo "📁 ダウンロードされた成果物を確認中..."
          echo "=== artifacts ディレクトリ構造 ==="
          ls -la ./artifacts/ || echo "❌ artifactsディレクトリが見つかりません"

          if [ -d "./artifacts/macos-artifacts" ]; then
            echo ""
            echo "=== MacOS成果物 ==="
            ls -la ./artifacts/macos-artifacts/
          else
            echo "⚠️ MacOS成果物が見つかりません"
          fi

          if [ -d "./artifacts/windows-artifacts" ]; then
            echo ""
            echo "=== Windows成果物 ==="
            ls -la ./artifacts/windows-artifacts/
          else
            echo "⚠️ Windows成果物が見つかりません"
          fi

      - name: 静的JSONファイルの生成
        run: |
          echo "🔧 Tauri自動アップデート用静的JSONファイルを生成中..."

          # 環境変数の設定
          export VERSION="${{ needs.version-tag.outputs.VERSION }}"
          export RELEASE_TAG="${{ needs.version-tag.outputs.RELEASE_TAG }}"
          export RELEASE_NOTES="バージョン ${{ needs.version-tag.outputs.VERSION }} のリリース"
          export GITHUB_REPOSITORY="${{ github.repository }}"

          # JSONファイル生成スクリプトの実行
          node script/generate-update-manifest.cjs

          echo "✅ 静的JSONファイルの生成が完了しました"

          # 生成されたファイルの確認
          echo "📁 生成されたマニフェストファイル:"
          if [ -d "update-manifests" ]; then
            ls -la update-manifests/
          else
            echo "❌ update-manifestsディレクトリが見つかりません"
            exit 1
          fi

      - name: リリースの作成とアップロード
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = require('path');

            const version = '${{ needs.version-tag.outputs.VERSION }}';
            const releaseTag = '${{ needs.version-tag.outputs.RELEASE_TAG }}';
            const commitShort = '${{ needs.version-tag.outputs.COMMIT_SHORT }}';

            console.log('🚀 リリース作成開始: ' + releaseTag);

            // 成果物ディレクトリの存在確認
            const artifactsDir = './artifacts';
            const macosArtifactsDir = path.join(artifactsDir, 'macos-artifacts');
            const windowsArtifactsDir = path.join(artifactsDir, 'windows-artifacts');

            console.log('📁 成果物ディレクトリの確認:');
            
            if (!fs.existsSync(artifactsDir)) {
              console.error('❌ artifactsディレクトリが見つかりません: ' + artifactsDir);
              throw new Error('artifactsディレクトリが見つかりません');
            }

            // 利用可能なディレクトリを表示
            const availableDirs = fs.readdirSync(artifactsDir);
            console.log('   利用可能なディレクトリ: ' + availableDirs.join(', '));

            if (!fs.existsSync(macosArtifactsDir)) {
              console.error('❌ MacOS成果物ディレクトリが見つかりません: ' + macosArtifactsDir);
              throw new Error('MacOS成果物ディレクトリが見つかりません');
            }

            if (!fs.existsSync(windowsArtifactsDir)) {
              console.error('❌ Windows成果物ディレクトリが見つかりません: ' + windowsArtifactsDir);
              throw new Error('Windows成果物ディレクトリが見つかりません');
            }

            // 成果物ファイルの検索（より柔軟な検索）
            const macosFiles = fs.readdirSync(macosArtifactsDir);
            const windowsFiles = fs.readdirSync(windowsArtifactsDir);
            
            const dmgFiles = macosFiles.filter(f => f.endsWith('.dmg') && !f.endsWith('.dmg.sig'));
            const msiFiles = windowsFiles.filter(f => f.endsWith('.msi') && !f.endsWith('.msi.sig'));
            const dmgSigFiles = macosFiles.filter(f => f.endsWith('.dmg.sig'));
            const msiSigFiles = windowsFiles.filter(f => f.endsWith('.msi.sig'));

            console.log('📦 検出された成果物:');
            console.log('   MacOS DMG: ' + dmgFiles.length + '個 (' + dmgFiles.join(', ') + ')');
            console.log('   MacOS署名: ' + dmgSigFiles.length + '個 (' + dmgSigFiles.join(', ') + ')');
            console.log('   Windows MSI: ' + msiFiles.length + '個 (' + msiFiles.join(', ') + ')');
            console.log('   Windows署名: ' + msiSigFiles.length + '個 (' + msiSigFiles.join(', ') + ')');

            if (dmgFiles.length === 0) {
              console.error('❌ DMGファイルが見つかりません');
              console.log('   MacOSディレクトリ内容: ' + macosFiles.join(', '));
              throw new Error('DMGファイルが見つかりません');
            }

            if (msiFiles.length === 0) {
              console.error('❌ MSIファイルが見つかりません');
              console.log('   Windowsディレクトリ内容: ' + windowsFiles.join(', '));
              throw new Error('MSIファイルが見つかりません');
            }

            // ファイルパスとサイズの取得
            const dmgPath = path.join(macosArtifactsDir, dmgFiles[0]);
            const msiPath = path.join(windowsArtifactsDir, msiFiles[0]);

            const dmgStats = fs.statSync(dmgPath);
            const msiStats = fs.statSync(msiPath);
            const dmgSizeMB = (dmgStats.size / 1024 / 1024).toFixed(2);
            const msiSizeMB = (msiStats.size / 1024 / 1024).toFixed(2);

            console.log('📊 ファイルサイズ:');
            console.log('   - MacOS: ' + dmgFiles[0] + ' (' + dmgSizeMB + ' MB)');
            console.log('   - Windows: ' + msiFiles[0] + ' (' + msiSizeMB + ' MB)');

            // リリースノートの生成
            const releaseNotes = [
              '## 🎉 新しいクロスプラットフォームリリース: ' + releaseTag,
              '',
              '### 📦 ダウンロード',
              '| プラットフォーム | ファイル | サイズ |',
              '|---|---|---|',
              '| **MacOS** | ' + dmgFiles[0] + ' | ' + dmgSizeMB + ' MB |',
              '| **Windows** | ' + msiFiles[0] + ' | ' + msiSizeMB + ' MB |',
              '',
              '### 🔄 自動アップデート',
              'このリリースには、Tauri自動アップデート機能用の静的JSONマニフェストファイルが含まれています：',
              '- `darwin-x86_64.json` - macOS Intel用',
              '- `darwin-aarch64.json` - macOS Apple Silicon用',
              '- `windows-x86_64.json` - Windows 64bit用',
              '',
              '既存のアプリケーションは自動的に新しいバージョンをチェックし、アップデートを通知します。',
              '',
              '### 📋 ビルド情報',
              '- **バージョン**: ' + version,
              '- **リリースタグ**: ' + releaseTag,
              '- **コミット**: ' + context.sha + ' (' + commitShort + ')',
              '- **ビルド日時**: ' + new Date().toLocaleString('ja-JP', { timeZone: 'Asia/Tokyo' }) + ' (JST)',
              '- **ビルド者**: ' + context.actor,
              '',
              '### 🔧 インストール方法',
              '',
              '#### MacOS',
              '1. dmgファイルをダウンロード',
              '2. dmgファイルをダブルクリックしてマウント',
              '3. アプリケーションをApplicationsフォルダにドラッグ&ドロップ',
              '',
              '#### Windows',
              '1. msiファイルをダウンロード',
              '2. msiファイルを右クリックして「管理者として実行」を選択',
              '3. インストールウィザードに従ってインストール'
            ].join('\n');

            // リリースの作成
            console.log('📝 GitHubリリースを作成中...');
            const release = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: releaseTag,
              name: 'オラの経費だゾ ' + releaseTag,
              body: releaseNotes,
              draft: false,
              prerelease: false
            });

            console.log('✅ リリースが作成されました: ' + release.data.html_url);

            // ファイルアップロード関数
            async function uploadFile(filePath, fileName, description) {
              try {
                console.log('📤 ' + description + 'をアップロード中: ' + fileName);
                const fileContent = fs.readFileSync(filePath);
                await github.rest.repos.uploadReleaseAsset({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  release_id: release.data.id,
                  name: fileName,
                  data: fileContent
                });
                console.log('✅ アップロード完了: ' + fileName);
              } catch (error) {
                console.error('❌ アップロード失敗: ' + fileName + ' - ' + error.message);
                throw error;
              }
            }

            // DMGファイルのアップロード
            await uploadFile(dmgPath, dmgFiles[0], 'MacOS DMGファイル');

            // DMG署名ファイルのアップロード
            if (dmgSigFiles.length > 0) {
              const dmgSigPath = path.join(macosArtifactsDir, dmgSigFiles[0]);
              await uploadFile(dmgSigPath, dmgSigFiles[0], 'MacOS署名ファイル');
            } else {
              console.warn('⚠️  MacOS署名ファイルが見つかりません');
            }

            // MSIファイルのアップロード
            await uploadFile(msiPath, msiFiles[0], 'Windows MSIファイル');

            // MSI署名ファイルのアップロード
            if (msiSigFiles.length > 0) {
              const msiSigPath = path.join(windowsArtifactsDir, msiSigFiles[0]);
              await uploadFile(msiSigPath, msiSigFiles[0], 'Windows署名ファイル');
            } else {
              console.warn('⚠️  Windows署名ファイルが見つかりません');
            }

            // 静的JSONファイルのアップロード
            console.log('📤 静的JSONマニフェストファイルをアップロード中...');
            const manifestDir = './update-manifests';
            if (fs.existsSync(manifestDir)) {
              const manifestFiles = fs.readdirSync(manifestDir).filter(f => f.endsWith('.json'));

              for (const manifestFile of manifestFiles) {
                const manifestPath = path.join(manifestDir, manifestFile);
                await uploadFile(manifestPath, manifestFile, 'マニフェストファイル');
              }

              console.log('✅ ' + manifestFiles.length + '個のマニフェストファイルをアップロードしました');
            } else {
              console.warn('⚠️  マニフェストディレクトリが見つかりません: ' + manifestDir);
            }

            console.log('🎉 クロスプラットフォームリリースが正常に作成されました！');
            console.log('🔗 リリースURL: ' + release.data.html_url);

  # ビルド結果のサマリー（PR用）
  build-summary:
    needs: [version-tag, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: ビルド結果のサマリー表示
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const version = '${{ needs.version-tag.outputs.VERSION }}';
            const releaseTag = '${{ needs.version-tag.outputs.RELEASE_TAG }}';
            const macosResult = '${{ needs.build-macos.result }}';
            const windowsResult = '${{ needs.build-windows.result }}';
            const eventName = '${{ github.event_name }}';

            console.log('='.repeat(60));
            console.log('🎯 クロスプラットフォームビルド結果サマリー');
            console.log('='.repeat(60));
            console.log('');
            console.log('📋 ビルド情報:');
            console.log('  - バージョン: ' + version);
            console.log('  - リリースタグ: ' + releaseTag);
            console.log('  - イベント: ' + eventName);
            console.log('');
            console.log('📊 ビルド結果:');
            console.log('  - MacOS: ' + (macosResult === 'success' ? '✅ 成功' : '❌ 失敗'));
            console.log('  - Windows: ' + (windowsResult === 'success' ? '✅ 成功' : '❌ 失敗'));
            console.log('');

            if (eventName === 'pull_request') {
              console.log('📝 PR用ビルドテスト:');
              console.log('  ✅ ビルドテストが完了しました');
              console.log('  ℹ️  リリースは作成されません（PR用テスト）');
              console.log('  💡 releaseブランチにマージ後、自動的にリリースが作成されます');
            } else if (macosResult === 'success' && windowsResult === 'success') {
              console.log('🚀 リリース作成:');
              console.log('  ✅ すべてのビルドが成功しました');
              console.log('  📦 リリース作成ジョブが実行されます');
            } else {
              console.log('⚠️  ビルド失敗:');
              console.log('  ❌ 一部のビルドが失敗したため、リリースは作成されません');
            }

            console.log('');
            console.log('='.repeat(60));
